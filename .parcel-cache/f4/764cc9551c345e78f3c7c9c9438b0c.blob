var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _viewJs = require("./View.js");
var _viewJsDefault = parcelHelpers.interopDefault(_viewJs);
var _iconsSvg = require("url:../../img/icons.svg");
var _iconsSvgDefault = parcelHelpers.interopDefault(_iconsSvg);
class PaginationView extends _viewJsDefault.default {
    _parentElement = document.querySelector('.pagination');
    _generateMarkup() {
        const numPages = Math.ceil(this._data.results.length / this._data.resultsPerPage);
        console.log(numPages);
        // Page 1 and there are other pages 
        if (this._data.page === 1 && numPages > 1) return "first pages and there are other pages ";
        // page 1 and there are ≈not other pages
        if (this._data.page === 1 && this._data.results.length < 10) // last page
        {
            if (this._data.page === numPages) return 'last pages';
        }
        // other  page 
        if (this._data.page < numPages) return 'other pages  ';
    }
}
exports.default = new PaginationView();
